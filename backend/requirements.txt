# ============================================================
# CodeMap — requirements.txt
# Install on ALL machines: pip install -r requirements.txt
# Python 3.11+ required
# ============================================================

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
websockets==13.0

# --- Task Queue ---
celery[redis]==5.4.0
redis==5.0.8
kombu==5.3.7

# --- Database ---
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0
psycopg2-binary==2.9.9

# --- Neo4j Graph DB ---
neo4j==5.24.0

# --- Static Analysis / AST ---
tree-sitter==0.23.1
tree-sitter-python==0.23.2
tree-sitter-javascript==0.23.0
tree-sitter-typescript==0.23.0
tree-sitter-go==0.23.0
tree-sitter-java==0.23.0
tree-sitter-rust==0.23.1

# --- Graph Algorithms ---
networkx==3.3

# --- Git Operations ---
gitpython==3.1.43

# --- HTTP Client (async) ---
httpx==0.27.2
aiohttp==3.10.5
aiofiles==24.1.0

# --- AI Clients ---
anthropic==0.34.2
groq==0.11.0

# --- Pydantic / Config ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Serialization ---
orjson==3.10.7
msgpack==1.1.0

# --- Logging / Monitoring ---
structlog==24.4.0
rich==13.8.1

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# --- Dev Tools ---
black==24.8.0
ruff==0.6.8


# ============================================================
# SYSTEM DEPENDENCIES (install separately — not via pip)
# ============================================================
# 
# 1. Ollama  (GPU machines — ALL THREE laptops)
#    curl -fsSL https://ollama.ai/install.sh | sh
#    ollama pull llama3.2
#    ollama pull nomic-embed-text
#
# 2. Redis   (Machine A only)
#    sudo apt install redis-server   OR   brew install redis
#
# 3. PostgreSQL  (Machine A only)
#    sudo apt install postgresql     OR   brew install postgresql
#
# 4. Neo4j   (Machine B only)
#    Download: https://neo4j.com/download-center/
#    OR: docker run -p 7474:7474 -p 7687:7687 neo4j:5
#
# 5. Docker (optional but recommended for Neo4j + Redis)
#    https://docs.docker.com/get-docker/
#
# ============================================================
# FRONTEND — run separately in /frontend directory
# ============================================================
#
#   npm install
#
# Key packages (already in package.json after npm install):
#   next@14                    — framework
#   three@0.168                — 3D graph rendering
#   @react-three/fiber         — React wrapper for Three.js
#   @react-three/drei          — helpers (OrbitControls, Html labels)
#   reactflow@12               — 2D drill-down subgraph
#   @dagrejs/dagre             — auto-layout for React Flow
#   swr                        — data fetching + caching
#   tailwindcss@3              — styling
#   @radix-ui/react-*          — headless UI primitives
#   lucide-react               — icons
#   zustand                    — lightweight state management
#   framer-motion              — UI animations
#
# ============================================================
